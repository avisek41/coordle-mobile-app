import {
  setCredentials,
  logOut,
  logIn,
  selectCurrentUserId,
} from './AuthSlice';
export { default as AuthSlice } from './AuthSlice';

export { setCredentials, logOut, logIn, selectCurrentUserId };
